name: Continuous Integration Check

on:
  push:
    branches:
      - main
      - task-1
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest # Run the job on a fresh Linux virtual machine

    steps:
    - name: Checkout code
      uses: actions/checkout@v4 # Get the code from the repo

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10' # Ensure a consistent Python version

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt # Use your committed requirements.txt

    - name: Basic Check (Optional, but good practice)
      run: |
        # Run a simple test or command here to verify basic code functionality
        # For now, we'll just confirm all files are present.
        ls